import { sql } from "drizzle-orm";
import { integer, sqliteTable, text } from "drizzle-orm/sqlite-core";
export const works = sqliteTable("works", { id: text("id").primaryKey(), canonicalTitle: text("canonical_title").notNull(), originalBengaliTitle: text("original_bengali_title").notNull(), fullText: text("full_text"), approvedExcerpt: text("approved_excerpt"), summary: text("summary"), articleType: text("article_type"), series: text("series"), topics: text("topics").notNull().default("[]"), tags: text("tags").notNull().default("[]"), author: text("author").notNull(), language: text("language").notNull().default("bn"), rightsStatus: text("rights_status").notNull(), publicationStatus: text("publication_status").notNull().default("draft"), createdAt: text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`), updatedAt: text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`) });
export const publicationOccurrences = sqliteTable("publication_occurrences", { id: text("id").primaryKey(), workId: text("work_id").notNull(), publication: text("publication").notNull(), edition: text("edition"), publicationDate: text("publication_date"), originalUrl: text("original_url"), pageNumber: text("page_number"), sourceKey: text("source_key"), verificationStatus: text("verification_status").notNull().default("pending"), dateConfidence: text("date_confidence"), notes: text("notes") });
export const sources = sqliteTable("sources", { id: text("id").primaryKey(), workId: text("work_id"), kind: text("kind").notNull(), location: text("location"), discoveryMethod: text("discovery_method"), evidenceNotes: text("evidence_notes"), rightsNotes: text("rights_notes"), createdAt: text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`) });
export const comments = sqliteTable("comments", { id: text("id").primaryKey(), workId: text("work_id").notNull(), name: text("name").notNull(), email: text("email"), body: text("body").notNull(), moderationStatus: text("moderation_status").notNull().default("pending"), createdAt: text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`) });
export const auditEvents = sqliteTable("audit_events", { id: integer("id").primaryKey({ autoIncrement: true }), entityType: text("entity_type").notNull(), entityId: text("entity_id").notNull(), action: text("action").notNull(), actorId: text("actor_id"), details: text("details"), createdAt: text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`) });
